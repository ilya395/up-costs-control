const supportMessageRoutes = require("./routes/support.route");
const supportMessageModel = require("./models/support.model");
const supportMessageController = require("./controllers/support.controller");

module.exports = {
  supportMessageRoutes,
  supportMessageModel,
  supportMessageController,
}